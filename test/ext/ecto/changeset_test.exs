defmodule Ext.Ecto.ChangesetTest do
  use GraphqlUsersApi.DataCase

  # alias GraphqlUsersApi.Accounts

  test "flatten errors" do
    assert true
    # errors =
    #   Accounts.create_user(%{})
    #   |> Ext.Ecto.Changeset.flatten_assoc()

    # assert "test", errors
  end
end
